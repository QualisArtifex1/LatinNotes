# Latin Text Annotator

This contains everything you need to run your app locally.

The app includes a bundled Open Words Latin dictionary for selected-word lookup in the annotation view.

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
